#include "main.h"

/**
 * _abs - calculate the absulut value of an interger
 * @i: declare abs of the number
 * Return: n or -n for negative number only absolut value
 */

int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
return (0);
}
