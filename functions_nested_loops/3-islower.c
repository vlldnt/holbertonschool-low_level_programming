#include "main.h"
#include <stdio.h>
/**
 * _islower - check lower case characters
 * @c: crotte
 * Return: 1 check is lower or capital
 */
int _islower(int c)
{
	if (c >= 97  && c <= 122)
		return (1);
return (0);
}
