#include "main.h"
#include <stdio.h>

/**
 * _islower - check lower case characters
 * Return: 1 check is lower or capital
 */
int _islower(int c)
{
	if (c >= 65 && c <= 90)
		return (0);
return (1);
}
