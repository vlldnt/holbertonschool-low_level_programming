#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - print 0 to 10
 * Return: O always
*/

int main(void)
{
	int x;

	for (x = 0; x <= 9; x++)
	{
		printf("%d", x);
	}
	{
		printf("\n");
	}
		return (0);
}
